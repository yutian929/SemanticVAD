name: x2-turn-dialogue
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - ffmpeg
  - libsndfile
  - openssl
  - curl
  - pip:
      - -e ./voxtral-realtime
      - -e ./full-duplex-demo[demo,llm]
